#include "main.h"
#include <stdio.h>
/**
 * main - function pointing to the prototype of displaying a function
 * Return: always 0
 */
int main(void)
{
	char *str;

	str = "put me out on display to be seen";
	_puts(str);
	str = "i do not fear computers  i fear the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}

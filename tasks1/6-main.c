#include "main.h"
#include <stdio.h>
/**
 * main the function that prints every other element ,even
 * Return: always 0
 */
int main(void)
{
	char *str;

	str = "0123456789";

	printf("%s\n", str);
	puts2(str);
	return (0);
}

